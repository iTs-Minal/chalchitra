import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['assets.aceternity.com','aceternity.com','images.unsplash.com', 'image.tmdb.org'],
  },
};

export default nextConfig;
